SELECT
    ibu_id AS athlete_id,
    race_id,
    event_id,
    bib,
    leg,
    start_order,
    result_order,
    rank,
    wc,
    nc,
    noc,
    shootings,
    shooting_total,
    run_time,
    total_time,
    behind,
    pursuit_start_distance,
    irm,
    is_team,
    result
FROM {{ ref('stg_race_results') }}